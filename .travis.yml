sudo: required
dist: trusty
language: java
jdk:
    - openjdk8
    - openjdk11
env:
    - RUNTIME=ol RUNTIME_VERSION=21.0.0.3
    - RUNTIME=ol RUNTIME_VERSION=20.0.0.12
    - RUNTIME=wlp RUNTIME_VERSION=21.0.0.3
    - RUNTIME=wlp RUNTIME_VERSION=20.0.0.12
script:
- travis_wait mvn verify -Druntime=$RUNTIME -DruntimeVersion=$RUNTIME_VERSION
